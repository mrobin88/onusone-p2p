# Force npm to resolve peer dependency conflicts
legacy-peer-deps=true
# Use exact versions for Solana packages
save-exact=false
# Allow overrides to work properly
force=false
# Ensure proper dependency resolution
auto-install-peers=true
